name: Trigger editor outline validation

on:
  pull_request:
    branches:
      - main

jobs:
  trigger-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR validation trigger is active"
