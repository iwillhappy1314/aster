name: Agent Outline Edit Sync Check

on:
  pull_request:
    branches:
      - main

jobs:
  cargo-check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo check
