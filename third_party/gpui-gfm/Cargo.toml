[package]
name = "gpui-gfm"
version = "0.2.0"
edition = "2024"
publish = false

[lib]
path = "lib.rs"

[features]
default = ["gpui-render"]
gpui-render = ["dep:gpui"]
html = []

[dependencies]
gpui = { version = "0.2.2", optional = true }
comrak = "0.50"
tl = "0.7"
syntect = "5.3.0"
